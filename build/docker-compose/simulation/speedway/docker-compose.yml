version: "3.8"
services:

    speedway-campbell:
        build:
            context: $MMITSS_ROOT/mmitss
            dockerfile: $MMITSS_ROOT/mmitss/build/dockerfiles/$PROCESSOR/Dockerfile.mrp-simulation
        container_name: speedway-campbell
        volumes:
          - type: bind
            source: $MMITSS_ROOT/mmitss/config/speedway/speedway-campbell/nojournal
            target: /nojournal
        networks:    
            mmitss_vlan:
                ipv4_address: 10.12.6.3    

    speedway-cherry:
        build:
            context: $MMITSS_ROOT/mmitss
            dockerfile: $MMITSS_ROOT/mmitss/build/dockerfiles/$PROCESSOR/Dockerfile.mrp-simulation
        container_name: speedway-cherry
        volumes:
            - type: bind
              source: $MMITSS_ROOT/mmitss/config/speedway/speedway-cherry/nojournal
              target: /nojournal
        networks:    
            mmitss_vlan:
                ipv4_address: 10.12.6.6

    speedway-mountain:
        build:
            context: $MMITSS_ROOT/mmitss
            dockerfile: $MMITSS_ROOT/mmitss/build/dockerfiles/$PROCESSOR/Dockerfile.mrp-simulation
        container_name: speedway-mountain
        volumes:
            - type: bind
              source: $MMITSS_ROOT/mmitss/config/speedway/speedway-mountain/nojournal
              target: /nojournal
        networks:    
            mmitss_vlan:
                ipv4_address: 10.12.6.9

    speedway-park:
        build:
            context: $MMITSS_ROOT/mmitss
            dockerfile: $MMITSS_ROOT/mmitss/build/dockerfiles/$PROCESSOR/Dockerfile.mrp-simulation
        container_name: speedway-park
        volumes:
            - type: bind
              source: $MMITSS_ROOT/mmitss/config/speedway/speedway-park/nojournal
              target: /nojournal
        networks:    
            mmitss_vlan:
                ipv4_address: 10.12.6.12

    simulation-tools:
        build:
            context: $MMITSS_ROOT/mmitss
            dockerfile: $MMITSS_ROOT/mmitss/build/dockerfiles/$PROCESSOR/Dockerfile.simulation-tools
        container_name: simulation-tools
        volumes:
          - type: bind
            source: $MMITSS_ROOT/mmitss/config/simulation-tools/nojournal
            target: /nojournal
        networks:    
            mmitss_vlan:
                ipv4_address: 10.12.6.20

networks:
    mmitss_vlan:
        driver: macvlan
        driver_opts:
            parent: $MMITSS_NETWORK_ADAPTER
        ipam:
            config:
              - subnet: 10.12.6.0/24


